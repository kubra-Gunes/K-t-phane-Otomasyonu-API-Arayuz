<UserControl x:Class="KutuphaneOtomasyonu.WPF.Views.UpdateCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:KutuphaneOtomasyonu.WPF.ViewModels"
             xmlns:local="clr-namespace:KutuphaneOtomasyonu.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:UpdateCategoryViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Kategori Güncelle"
                   Style="{StaticResource TitleText}"
                   Margin="0,0,0,20"
                   Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Kategori Seç" FontWeight="SemiBold"/>
            <ComboBox ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      DisplayMemberPath="KategoriAdi"
                      Padding="8"/>
        </StackPanel>

        <Border Grid.Row="2"
                Style="{StaticResource CardStyle}"
                Margin="0,0,0,20"
                Padding="20">
            <StackPanel>
                <TextBlock Text="Güncellenecek Kategori Bilgileri" Style="{StaticResource SubtitleText}" Margin="0,0,0,10"/>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Kategori ID" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding UpdatedCategory.KategoriId, Mode=OneWay}"
                             IsReadOnly="True"
                             Background="#EEEEEE"
                             Padding="8"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,15">
                    <TextBlock Text="Kategori Adı" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding UpdatedCategory.KategoriAdi, UpdateSourceTrigger=PropertyChanged}"
                             Padding="8"/>
                </StackPanel>

                <Button Content="Kategoriyi Güncelle"
                            Command="{Binding UpdateCategoryCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Margin="0,0,0,10"
                            Width="220" 
                            Background="#2E8B57" 
                            Foreground="White" 
                            Height="40"
                            FontSize="14"/>

                <Button Content="Kategoriyi Sil" 
                            Command="{Binding DeleteCategoryCommand}" 
                            Background="#DC3545" 
                            Foreground="White" 
                            FontWeight="Bold"
                            Padding="10,5"
                            BorderBrush="#DC3545" 
                            BorderThickness="1"
                            Cursor="Hand" 
                            Width="220"  
                            Height="40"
                            FontSize="14"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0,0,0,10"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
        <TextBlock Grid.Row="4"
                   Text="{Binding SuccessMessage}"
                   Foreground="Green"
                   Margin="0,0,0,10"
                   Visibility="{Binding SuccessMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

        <ProgressBar Grid.Row="5"
                     IsIndeterminate="True"
                     Height="10"
                     VerticalAlignment="Top"
                     Margin="0,10,0,0"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>