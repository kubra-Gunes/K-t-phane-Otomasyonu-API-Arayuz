<UserControl x:Class="KutuphaneOtomasyonu.WPF.Views.ReturnBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="#f5f7fa">

    <UserControl.Resources>
        <!-- Buton stili -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2563eb"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1e40af"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox stili -->
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Bu şekilde bırakın (Template satırını silin) -->
                            <ToggleButton Name="ToggleButton"
              Grid.Column="2"
              Focusable="false"
              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
              ClickMode="Press"/>


                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="6,2,25,2"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>

                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MaxHeight="300"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      Background="White"
                                      SnapsToDevicePixels="True">
                                    <Border BorderThickness="1" BorderBrush="#cbd5e1" CornerRadius="4">
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header TextBlock -->
        <Style x:Key="HeaderTextBlock" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Kitap İade Et" Style="{StaticResource HeaderTextBlock}" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="İade Edilecek Ödünç İşlemini Seçin:" 
                       FontWeight="SemiBold" FontSize="14" Foreground="#475569" Margin="0,0,0,5"/>

            <ComboBox ItemsSource="{Binding BorrowedBooks}"
                      SelectedItem="{Binding SelectedBorrowedBook}"
                      Width="400" Height="30" HorizontalAlignment="Left"
                      ToolTip="Ödünç alınmış kitaplardan birini seçin.">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ItemBorder" Padding="5" CornerRadius="4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding KitapAdi}" FontWeight="SemiBold" Foreground="#111827"/>
                                <TextBlock Text=" - " Foreground="#64748b"/>
                                <TextBlock Text="Kullanıcı: " Foreground="#64748b"/>
                                <TextBlock Text="{Binding KullaniciEmail}" Foreground="#1e3a8a"/>
                                <TextBlock Text=" (Ödünç: " Foreground="#64748b"/>
                                <TextBlock Text="{Binding OduncAlmaTarihi, StringFormat='dd.MM.yyyy'}" Foreground="#1e3a8a"/>
                                <TextBlock Text=")" Foreground="#64748b"/>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#fecaca"/>
                                <!-- LightCoral tonu -->
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <Button Content="İade Et" Grid.Row="2" HorizontalAlignment="Left"
                Style="{StaticResource PrimaryButton}" 
                Command="{Binding ReturnBookCommand}"
                Background="#2E8B57" Foreground="White"
                Width="150"
                Height="40"/>

        <TextBlock Text="{Binding StatusMessage}" Grid.Row="3" Margin="0,20,0,0"
                   Foreground="#dc2626" FontStyle="Italic" FontSize="13"/>
    </Grid>
</UserControl>
